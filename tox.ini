[tox]
envlist =py312
isolated_build = true

[testenv]
deps = 
    -rrequirements-dev.txt
commands =
    pytest

[flake8]
max-line-length = 88
exclude = 
    .git,
    __pycache__,
    .venv,
    venv,
    build,
    dist